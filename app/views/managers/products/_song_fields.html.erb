<div class="nested-fields">
    <td><%= f.select :sequence, options_for_select(1..30)%></td>
    <td>
        <%= f.text_field :name %>
        <%= link_to_remove_association "曲を削除", f, class: 'btn btn-sm btn-danger' %>
    </td>
</div>
